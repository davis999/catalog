spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reactivesw
    username: root
    password: root
    driverClassName: com.mysql.jdbc.Driver
  jpa: 
    database: MYSQL
    hibernate: 
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    properties: 
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  profiles:
    active: prod
    
grpc: 
  port: 9096